NEBIUS_API_KEY=your_nebius_api_key_here

# Optional: keep your workspace between runs by saving it to a local JSON file.
# Leave unset on shared/public deployments so each visitor's data stays in their own session.
# WORKSPACE_FILE=workspace.json
